[package]
name = "spectre-core"
version = "0.1.0"
edition = "2021"
description = "Stateless password derivation (Spectre / Master Password algorithm v3)."
license = "MIT"

[dependencies]
scrypt = "0.11"
hmac = "0.12"
sha2 = "0.10"
zeroize = "1"
